#Play1
- name: This is our first play
  hosts: webservers
  become: true
  tasks:
    - name: install packages
      apt: 
        name: "{{ item }}"
        state: latest
      with_items: 
         - vsftpd
         - tree  

 
